# GENERATED by scripts/generate_enterprise_compose.sh — container-per-tenant-user.
# One container per TENANT USER; never two services for the same user_id
# (per-user SQLite/ChromaDB are single-writer; AGENTS.md).
#
# Auth models: per-container API_KEY (set in each user's .env) OR
# PER_USER_AUTH=true on every container with keys minted once centrally.
services:

  assistant-ent-908bd07a:
    image: ghcr.io/open-assistants-lab/assistant:latest
    container_name: assistant-ent-908bd07a
    command: ["uv", "run", "assistant", "http"]
    ports:
      - "127.0.0.1:8122:8080"
    environment:
      API_HOST: "0.0.0.0"
      API_PORT: "8080"
      DEPLOYMENT_DATA_ROOT: /app/data
      DEPLOYMENT_DATA_PATH: /app/data
      DEPLOYMENT_MODE: enterprise
    env_file:
      - ./enterprise/users/smoke_alice/.env
    volumes:
      - ./enterprise/users/smoke_alice/data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3

  assistant-ent-a491c25c:
    image: ghcr.io/open-assistants-lab/assistant:latest
    container_name: assistant-ent-a491c25c
    command: ["uv", "run", "assistant", "http"]
    ports:
      - "127.0.0.1:8192:8080"
    environment:
      API_HOST: "0.0.0.0"
      API_PORT: "8080"
      DEPLOYMENT_DATA_ROOT: /app/data
      DEPLOYMENT_DATA_PATH: /app/data
      DEPLOYMENT_MODE: enterprise
    env_file:
      - ./enterprise/users/smoke_bob/.env
    volumes:
      - ./enterprise/users/smoke_bob/data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
